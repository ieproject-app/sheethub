
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    // --- GLOBAL HELPER FUNCTIONS ---

    function isSignedIn() {
      return request.auth != null;
    }

    // --- COLLECTION RULES ---

    // Aturan Publik (Baca Saja)
    match /blogPosts_published/{blogPostId} {
      allow get, list: if true;
      allow create, update, delete: if false;
    }

    match /notes_published/{noteId} {
      allow get, list: if true;
      allow create, update, delete: if false;
    }

    match /authors/{authorId} {
      allow get, list: if true;
      allow create, update, delete: if false;
    }

    match /tags/{tagId} {
      allow get, list: if true;
      allow create, update, delete: if false;
    }

    match /media_library/{mediaId} {
      allow get, list: if true;
      allow create, update, delete: if false;
    }

    // Aturan untuk Tool Internal (Contoh: Number Generator)
    match /availableNumbers/{numberId} {
      allow get, list: if isSignedIn();
      allow update: if isSignedIn();
    }

    match /userGenerationLimits/{userId} {
      allow get: if isSignedIn() && request.auth.uid == userId;
      allow write: if isSignedIn() && request.auth.uid == userId;
    }
  }
}
