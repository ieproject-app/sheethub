'use client';

import React from 'react';

/**
 * NotePageClient is no longer responsible for rendering MDX.
 * Rendering MDX moved to page.tsx (Server Component) to avoid Next.js 15 Client Component errors.
 */
export function NotePageClient() {
  return null;
}